// part of Aliases.h   - bw 20090703#if ndef _DEFINEDINCARBON#define AliasHandle as Handle // ^^AliasRecord#endiftoolbox fn FSNewAlias( const FSRef * fromFile, const FSRef * target, AliasHandle * inAlias ) = OSErr // fromFile can be NULLtoolbox fn FSNewAliasMinimal( const FSRef * target, AliasHandle * inAlias ) = OSErrtoolbox fn FSResolveAlias( const FSRef * fromFile, AliasHandle alias, FSRef * target, Boolean * wasChanged ) = OSErrtoolbox fn FSResolveAliasFile( const FSRef * theRef, Boolean resolveAliasChains, Boolean *targetIsFolder, Boolean *wasAliased ) = OSErr