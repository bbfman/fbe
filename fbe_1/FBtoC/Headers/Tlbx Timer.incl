/*     File:       Timer.h  */toolbox Microseconds( UnsignedWide * microTickCount ) #if 0 // rp 20090806#if def _DEFINEDINCARBONbegin enum_kTMTaskActive = (1 << 15) /* high bit of qType is set if task is active */end enumbegin record TMTaskdim as ptr   qLinkdim as short qTypedim as proc  tmAddr// TimerUPPdim as long  tmCountdim as long  tmWakeUpdim as long  tmReservedend record#define TMTaskPtr as pointer to TMTask#endif /* _DEFINEDINCARBON */'~InterfaceLib 7.1 / CarbonLib 1.0toolbox InsTime   (TMTaskPtr QElemPtr) toolbox InsXTime  (TMTaskPtr QElemPtr) toolbox RmvTime   (TMTaskPtr QElemPtr) toolbox PrimeTime (TMTaskPtr QElemPtr, long count) '~InterfaceLib 9.1 / CarbonLib 1.0.2/*     InstallTimeTask, InstallXTimeTask, PrimeTimeTask and RemoveTimeTask     work just like InsTime, InsXTime, PrimeTime, and RmvTime except that     they return an OSErr result.*/toolbox fn InstallTimeTask  (TMTaskPtr QElemPtr) = OSErr toolbox fn InstallXTimeTask (TMTaskPtr QElemPtr) = OSErr toolbox fn PrimeTimeTask    (TMTaskPtr QElemPtr, long count) = OSErrtoolbox fn RemoveTimeTask   (TMTaskPtr QElemPtr) = OSErrtoolbox fn NewTimerUPP( ptr ) = ptrtoolbox DisposeTimerUPP( ptr )toolbox InvokeTimerUPP( TMTaskPtr tmTaskPtr, ptr TimerUPP )#endif